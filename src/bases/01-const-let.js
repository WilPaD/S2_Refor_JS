let nombre = "Wilbert" //Usamos let para definir una variable que puede cambiar a lo largo del programa
const apellido = "Padrón" //Usamos const para definir una variable constante (Que no cambiará)


console.log(nombre, apellido)

if (true){
    nombre = 'José'
}

console.log(nombre) //Imprimimos la variable let pues cambió valor a lo largo del programa
