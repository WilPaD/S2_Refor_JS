
let firstName 
let lastName = "Padrón"

/** Verifica si la variable tiene un valor, imprime dependiendo el contenido de la variable  */
//console.log(`${ firstName || 'No first name' } ${ lastName || 'No last name'}`)

const isActive = true 

let message = (isActive ) ? 'Activo' : 'Inactivo' //Operador ternario (If )
/*
if( isActive ){
    message = 'Activo'
}else{
    message 'Inactivo'
}
*/

console.log(message)